= title "Add a Lab"

= simple_form_for @lab do |f|
  - if @lab.errors.any?
    .error_messages
      %h2 Form is invalid
      %ul
        - @lab.errors.full_messages.each do |message|
          %li= message

  = f.input :name
  = f.input :description
  .actions= f.submit "Add Lab"